package org.kjots.json.`object`.test.feature

import org.junit.runner.RunWith

import org.scalatest.FeatureSpec
import org.scalatest.junit.JUnitRunner

/**
 * Create JSON Object Specification.
 * <p>
 * Created: 14th September 2011.
 * 
 * @author <a href="mailto:kjots@kjots.org">Karl J. Ots &lt;kjots@kjots.org&gt;</a>
 * @since 1.2
 */
@RunWith(classOf[JUnitRunner])
class CreateJsonObjectFeatureSpec extends FeatureSpec {
  feature("The user can create a new JSON Object") {
    scenario("A new JSON Object is created") (pending)
    scenario("A new JSON Object has no properties") (pending)
  }
}
